<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\Schema;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     * Hardcoded static seeders removed as requested.
     * All data is dynamically saved & fetched via RESTful APIs and Admin/Local controls.
     */
    public function run(): void
    {
        // Dynamic system: all records are created via REST APIs and Admin Forms
    }
}
